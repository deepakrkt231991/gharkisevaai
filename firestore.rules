rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: This is a temporary, insecure rule for debugging only.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
